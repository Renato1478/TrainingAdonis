const { hooks } = require('@adonisjs/ignitor')
const edge = require('edge.js')

// edge.global('method', function (method) {
//   return this.safe(`<input type="hidden" value="${method}" name="_method">`)
// })